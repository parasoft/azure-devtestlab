# Systemd unit file for CTS delay
[Unit]
Description=Delay CTS startup for 30 seconds after starting CTP

[Service]
Type=oneshot

ExecStartPre=/usr/bin/sleep 30
ExecStart=-/usr/bin/curl -f --silent -o - "http://localhost:8080/em/healthcheck"

User=cts
Group=parasoft

WorkingDirectory=/var/tomcat/cts

[Install]
WantedBy=cts.service
